# CD — Build, push to ECR, deploy to ECS Fargate (rolling update).
# Triggers on push to main (release merges from develop via git-flow) and only proceeds once
# the CI workflow has gone green for the same commit (see the require-ci gate below).
# Deploy units: ① API monolith (backend) · ② Ingestion worker · ④ Frontend.
# Each release also runs `cdk deploy` on the app-tier stacks (cdk-deploy job) so infra changes
# (IAM/env/autoscaling) ship with the release, not just the container image. Data/network/access
# stacks are excluded (--exclusively) and stay manual. Requires the CD role's bootstrap-role
# assume grant (cicd_stack.py), which itself must be applied once via a manual `cdk deploy
# Docsuri-CICD` (the CD role cannot widen its own permissions — chicken-and-egg).
name: CD

# DISABLED 2026-08-18 — there is no AWS account left to deploy to. Every resource in
# 559352512800 was destroyed (0 stacks, 0 ECR repos, no CDK bootstrap), and the service now
# runs on a single machine behind a Cloudflare Tunnel (see ops/server/README.md). Left intact
# rather than deleted: this file is the record of how the ECS/CDK deployment worked, and is
# the starting point if the service ever moves back to a cloud account.
#
# BOTH automatic entry points are removed on purpose:
#   • push:tags ["v*"] — a manually pushed v* tag would otherwise deploy. (The tag that
#     release.yml creates never triggered this: GitHub's anti-recursion rule suppresses
#     workflow triggers for refs created with GITHUB_TOKEN.)
#   • workflow_call   — release.yml's `deploy` job, which is removed there in the same commit.
# workflow_dispatch is kept so the deployment can still be invoked deliberately by a human
# who has re-provisioned an account; it will fail at assume-role until one exists.
#
# TO RE-ENABLE: restore `push: tags: ["v*"]` and `workflow_call: {}` here, restore the
# `deploy` job in release.yml, and re-run the manual `cdk deploy Docsuri-CICD` bootstrap
# described in the header above (the CD role cannot widen its own permissions).
on:
  workflow_dispatch: {}
  workflow_call: {} # retained so a restored release.yml deploy job works without editing here

# Two entry points, both 1:1 with a release:
#   • push a v* tag directly (hotfix) → OIDC sub = repo:...:ref:refs/tags/v*
#   • release.yml (on push:main) auto-tags then calls this as a reusable workflow → OIDC sub =
#     repo:...:ref:refs/heads/main (the caller's branch ref, NOT a tag).
# The CD role trust (cicd_stack.py) must therefore allow BOTH subs, else the reusable-call path
# fails at sts:AssumeRoleWithWebIdentity. All deploy jobs use env.CD_ROLE_ARN below.
permissions:
  contents: read
  id-token: write # OIDC for AWS assume-role (no long-lived keys)
  actions: read # require-ci reads the CI workflow's run status via the API

env:
  AWS_REGION: ap-northeast-2
  # Non-secret infra identifiers, inlined so CD needs no GitHub org/repo variables (setting org
  # vars requires admin:org, which we don't have). The account id is already public in
  # ops/cdk/app.py, and the CD role is assumable ONLY via OIDC from this repo on a v* tag — knowing
  # the ARN grants nothing. Change these here if the account/region/role ever moves.
  ECR_REGISTRY: 559352512800.dkr.ecr.ap-northeast-2.amazonaws.com
  CD_ROLE_ARN: arn:aws:iam::559352512800:role/docsuri-github-actions-cd
  IMAGE_SHA: ${{ github.sha }}

# Serialize ALL prod deploys (fixed group, never cancel an in-flight rollout) so two releases
# tagged in quick succession can't race each other's ECS deployments. (audit Low #8)
concurrency:
  group: cd-production
  cancel-in-progress: false

jobs:
  require-ci:
    name: Require CI green for this commit
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      # Gate: require CI green for THIS commit before deploying. The tagged commit was already
      # built on its push to main/develop, so this usually finds a completed CI run immediately; it
      # still polls (and fails closed on non-success/timeout) in case the tag was pushed with
      # --follow-tags before that run finished. Uses the preinstalled gh CLI. (audit High #3)
      - name: Wait for CI to succeed
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -uo pipefail
          for i in $(seq 1 80); do
            state=$(gh api \
              "repos/${{ github.repository }}/actions/runs?head_sha=${{ github.sha }}&per_page=100" \
              --jq '[.workflow_runs[] | select(.name=="CI")] | sort_by(.created_at)
                    | (last // {}) | ((.status // "queued") + ":" + (.conclusion // "null"))' \
              2>/dev/null || echo "api-error:none")
            echo "attempt $i — CI: $state"
            case "$state" in
              completed:success) echo "✅ CI passed — proceeding to deploy."; exit 0 ;;
              completed:*) echo "❌ CI concluded '$state' — refusing to deploy."; exit 1 ;;
            esac
            sleep 20
          done
          echo "⏱️ Timed out waiting for CI to finish — refusing to deploy."; exit 1

  build-push-api:
    name: Build & push — deploy unit ① (API)
    needs: require-ci
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5
      - uses: aws-actions/configure-aws-credentials@254c19bd240aabef8777f48595e9d2d7b972184b # v6
        with:
          role-to-assume: ${{ env.CD_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
      - uses: aws-actions/amazon-ecr-login@d539f0932e70871a027e9d5a9d8fc38589180a64 # v2
      - name: Build and push
        run: |
          REPO="$ECR_REGISTRY/docsuri-api"
          # Tag BOTH the immutable SHA (audit/rollback trail) and :latest. The ECS task definition
          # resolves tag="latest" (compute_stack.py), so pushing :latest is what makes the
          # force-new-deployment below actually roll out THIS build — without it the freshly built
          # image was orphaned in ECR and the deploy re-pulled the old :latest. (audit High #1)
          docker build -f backend/Dockerfile -t "$REPO:$IMAGE_SHA" -t "$REPO:latest" .
          docker push "$REPO:$IMAGE_SHA"
          docker push "$REPO:latest"
          docker inspect --format='pushed {{index .RepoDigests 0}}' "$REPO:$IMAGE_SHA"

  build-push-ingestion:
    name: Build & push — deploy unit ② (Ingestion)
    needs: require-ci
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5
      - uses: aws-actions/configure-aws-credentials@254c19bd240aabef8777f48595e9d2d7b972184b # v6
        with:
          role-to-assume: ${{ env.CD_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
      - uses: aws-actions/amazon-ecr-login@d539f0932e70871a027e9d5a9d8fc38589180a64 # v2
      - name: Build and push
        run: |
          REPO="$ECR_REGISTRY/docsuri-ingestion"
          docker build -f ingestion/Dockerfile -t "$REPO:$IMAGE_SHA" -t "$REPO:latest" .
          docker push "$REPO:$IMAGE_SHA"
          docker push "$REPO:latest"
          docker inspect --format='pushed {{index .RepoDigests 0}}' "$REPO:$IMAGE_SHA"

  build-push-frontend:
    name: Build & push — deploy unit ④ (Frontend)
    needs: require-ci
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5
      - uses: aws-actions/configure-aws-credentials@254c19bd240aabef8777f48595e9d2d7b972184b # v6
        with:
          role-to-assume: ${{ env.CD_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
      - uses: aws-actions/amazon-ecr-login@d539f0932e70871a027e9d5a9d8fc38589180a64 # v2
      - name: Build and push
        run: |
          REPO="$ECR_REGISTRY/docsuri-frontend"
          docker build -f frontend/Dockerfile -t "$REPO:$IMAGE_SHA" -t "$REPO:latest" frontend/
          docker push "$REPO:$IMAGE_SHA"
          docker push "$REPO:latest"
          docker inspect --format='pushed {{index .RepoDigests 0}}' "$REPO:$IMAGE_SHA"

  cdk-deploy:
    name: Deploy infra (cdk deploy — app-tier stacks)
    # Applies infra changes (IAM/env/autoscaling/new resources) that a plain image rebuild +
    # force-new-deployment can NOT — e.g. a task-role policy grant lives in CloudFormation, not the
    # running task set. Scoped with --exclusively to app-tier stacks so a release never churns
    # Network / Search (OpenSearch) / Access / CICD (data + IAM-topology stacks stay manual and
    # deliberate). Uses the CD role's bootstrap-role assume grant (cicd_stack.py); the ECS image
    # roll (deploy job) waits on this so infra lands before the new image.
    needs: [build-push-api, build-push-ingestion, build-push-frontend]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5
      - uses: aws-actions/configure-aws-credentials@254c19bd240aabef8777f48595e9d2d7b972184b # v6
        with:
          role-to-assume: ${{ env.CD_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "20"
      - name: Install CDK CLI + app deps
        run: |
          npm install -g aws-cdk@2
          python3 -m pip install --upgrade pip
          python3 -m pip install -r ops/cdk/requirements.txt
      - name: cdk deploy (app-tier stacks only)
        working-directory: ops/cdk
        run: |
          cdk deploy \
            Docsuri-Compute Docsuri-Ingestion Docsuri-Summarization \
            Docsuri-Novelty Docsuri-Evidence Docsuri-Frontend \
            --exclusively --require-approval never --concurrency 2 --no-color

  deploy:
    name: Deploy to ECS (rolling update)
    needs: [cdk-deploy]
    runs-on: ubuntu-latest
    # ponytail: deliberately NO `environment: production`. Binding a job to a GitHub Environment
    # rewrites the OIDC token sub to `repo:80-hours-a-week/DocSuri:environment:production`, which
    # the CD role trust policy (scoped to `...:ref:refs/tags/v*`) rejects → the deploy job fails at
    # sts:AssumeRoleWithWebIdentity while the build-push jobs (tag-ref sub) succeed. Re-add ONLY
    # after the role trust policy is widened to also allow the environment sub. (was audit Med #7)
    timeout-minutes: 30
    steps:
      - uses: aws-actions/configure-aws-credentials@254c19bd240aabef8777f48595e9d2d7b972184b # v6
        with:
          role-to-assume: ${{ env.CD_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      # force-new-deployment restarts each service; because the task definitions reference
      # :latest and we just pushed a new :latest, the new tasks pull THIS build's image.
      - name: Deploy API service (deploy unit ①)
        run: |
          aws ecs update-service --cluster docsuri --service docsuri-api \
            --force-new-deployment --no-cli-pager
          echo "API service deployment initiated."

      - name: Deploy Ingestion worker (deploy unit ②)
        run: |
          aws ecs update-service --cluster docsuri --service docsuri-ingestion \
            --force-new-deployment --no-cli-pager
          echo "Ingestion worker deployment initiated."

      - name: Deploy Novelty worker
        run: |
          aws ecs update-service --cluster docsuri --service docsuri-novelty-agent-worker \
            --force-new-deployment --no-cli-pager
          echo "Novelty worker deployment initiated."

      # The summary worker reuses the docsuri-api image (summarization_stack) and is the WRITER for
      # async long-summary + full-translation results; the API service is the READER. It has no
      # build job of its own (shares the API image) and is scale-to-zero, so nothing here refreshed
      # it — a release could move the API's async cache-key/result contract forward while the worker
      # kept running an older image, so it wrote results under a key the API never read and every
      # long summary + full translation sat in permanent "pending". Force a new deployment so the
      # worker pulls the same freshly-pushed :latest as every other unit, keeping the writer/reader
      # in lockstep on each release.
      - name: Deploy Summary worker (deploy unit ④)
        run: |
          aws ecs update-service --cluster docsuri --service docsuri-summary-worker \
            --force-new-deployment --no-cli-pager
          echo "Summary worker deployment initiated."

      - name: Deploy Frontend (deploy unit ④)
        run: |
          aws ecs update-service --cluster docsuri-frontend --service docsuri-frontend \
            --force-new-deployment --no-cli-pager
          echo "Frontend deployment initiated."

      - name: Wait for services to stabilize
        run: |
          aws ecs wait services-stable --cluster docsuri \
            --services docsuri-api docsuri-ingestion docsuri-novelty-agent-worker \
            docsuri-summary-worker --no-cli-pager
          aws ecs wait services-stable --cluster docsuri-frontend \
            --services docsuri-frontend --no-cli-pager
          echo "✅ API, ingestion, novelty worker, summary worker and frontend services stable."

      - name: Rollback on failure
        if: failure()
        run: |
          echo "⚠️ Deployment failed — the ECS deployment circuit breaker (rollback=true, set in"
          echo "   compute_stack.py) reverts each service to its last-stable task set automatically."
          echo "   Check the ECS console for circuit-breaker status."
